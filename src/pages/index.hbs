<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="/style.css">
  <title>Black Economic Trends Dashboard</title>
</head>
<body>
  <h1>Black Economic Trends Dashboard</h1>

  <h2>Median Weekly Earnings</h2>
  {{#if wageData}}
    <table>
      <thead>
        <tr>
          <th>Group</th>
          <th>Latest Earnings</th>
        </tr>
      </thead>
      <tbody>
        {{#each wageData}}
          <tr>
            <td>{{@key}}</td>
            <td>{{this.[this.length - 1].value}}</td>
          </tr>
        {{/each}}
      </tbody>
    </table>
  {{else}}
    <p>No wage data available.</p>
  {{/if}}

  <h2>Wage Gap (Black vs. White)</h2>
  {{#if wageGap}}
    <p>The wage gap ratio is: {{wageGap}}</p>
  {{else}}
    <p>Wage gap data unavailable.</p>
  {{/if}}

  <h2>Labor Force Participation Rate</h2>
  {{#if laborForceData}}
    <table>
      <thead>
        <tr>
          <th>Date</th>
          <th>Participation Rate</th>
        </tr>
      </thead>
      <tbody>
        {{#each laborForceData}}
          <tr>
            <td>{{this.date}}</td>
            <td>{{this.value}}</td>
          </tr>
        {{/each}}
      </tbody>
    </table>
  {{else}}
    <p>No labor force data available.</p>
  {{/if}}

  <!-- Debugging Placeholder -->
  <h2>Debugging Data</h2>
  <pre>{{{json wageData}}}</pre>
  <pre>{{{json laborForceData}}}</pre>
</body>
</html>